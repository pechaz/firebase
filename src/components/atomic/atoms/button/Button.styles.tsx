import { Button, styled as MUIStyled } from "@mui/material";

export const CustomizedButton = MUIStyled(Button)(() => ({
  // fontSize: "16px",
  // fontWeight: 500,
  // borderRadius: "4px",
  // padding: "12px 16px",
}));
